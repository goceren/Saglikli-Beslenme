<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sagliklibeslen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YedigimEkleActivity" />
        <activity android:name=".OzetActivity" />
        <activity android:name=".BesinGuncelle" />
        <activity android:name=".BesinEkleActivity" />
        <activity android:name=".AlarmActivity" />
        <activity
            android:name=".AyarlarActivity"
            android:theme="@style/AppTheme.FullScreen" />
        <activity android:name=".BesinlerActivity" />
        <activity
            android:name=".DetailActivity"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.FullScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name = ".MyNotificationPublisher" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>